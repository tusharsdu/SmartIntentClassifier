# Small Intent Classification Chatbot

## Steps to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Clean data: `python -m src.data_processing`
3. Train model: `python -m src.model_training`
4. Run Streamlit app: `streamlit run streamlit_app/app.py`
